name: Trigger KindleEar Push

on:
  push:
    branches: [ master ]
  schedule:
    - cron: '0 12 * * *'
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Call PythonAnywhere deliver endpoint
        run: |
          curl "https://w2700681.pythonanywhere.com/worker?key=cY9gKC&now=1"
